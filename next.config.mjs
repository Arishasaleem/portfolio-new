/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
     // Allow GitHub profile images 
    
        domains: ['avatars.githubusercontent.com', 'images.unsplash.com'], // Add both domains if needed

    }
};
// next.config.js
 
  
export default nextConfig;
